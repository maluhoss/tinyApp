<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp - Your ShortURL</title>
</head>
<body style="font-size:20px; padding:50px;">
  <% include partials/_header %>
  <a href='/urls/new'>Create New TinyURL</a>
  <% if (!user_id) {%>
  <p>Please log in to see the URL or this URL is not yours!</p>
  <%} else { %>

  <div>
  <h2> ShortURL: <b><%=shortURL%></b></h2>
  <form method="POST" action="/urls/<%=shortURL%>">
    <input class="form-control" type="text" name="longURL" placeholder="<%=longURL%>" style="width: 300px; margin: 1em">
    <button type="submit" class="btn btn-primary">Update URL</button>
    </div>
    <br>
    <form method="GET" action="/urls">
    <button type="submit" class="btn btn-primary">Go back to list of URLs</button>
  </form>
  <%}%>
</body>
</html>
